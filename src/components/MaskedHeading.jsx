import {useEffect,useRef} from 'react';import gsap from 'gsap';import './MaskedHeading.css';
export default function MaskedHeading({text,src,className='',trigger='view'}){const root=useRef(),media=useRef();useEffect(()=>{const el=root.current;const enter=()=>gsap.fromTo(el,{clipPath:'inset(0 100% 0 0)',y:35},{clipPath:'inset(0 0% 0 0)',y:0,duration:1.35,ease:'power4.inOut'});if(trigger==='mount')enter();else{const io=new IntersectionObserver(e=>{if(e[0].isIntersecting){enter();io.disconnect()}},{threshold:.25});io.observe(el);return()=>io.disconnect()}},[trigger]);useEffect(()=>{const el=root.current;const move=e=>{const r=el.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;gsap.to(media.current,{x:x*-32,y:y*-18,duration:.8,ease:'power3.out'})};el.addEventListener('pointermove',move);return()=>el.removeEventListener('pointermove',move)},[]);return <h2 ref={root} className={`masked-heading ${className}`} style={{backgroundImage:`url(${src})`}}><span ref={media}>{text}</span></h2>}
